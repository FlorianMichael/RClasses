plugins {
    id "java"
}

archivesBaseName = "RClasses"
version = "1.2.3"
group = "de.florianmichael"

repositories {
    mavenCentral()
}

dependencies {
}

java {
    withSourcesJar()

    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

jar {
    from("LICENSE") { rename { "${it}_${project.archivesBaseName}" } }
}
